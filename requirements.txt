requests>=2.31.0
python-dotenv>=1.0.0
jupyterlab>=4.0.0
ipywidgets>=8.1.0
pandas>=2.1.0
matplotlib>=3.8.0
folium>=0.15.0
pillow>=10.0.0
# scripts/fetch_approach.py, for the diffusion that closes NAIP's voids.
scipy>=1.11.0
shapely>=2.0.0
# Surface model from the 2017 airborne LiDAR (heatcanyon/lidar.py).
pyproj>=3.6.0
laspy[lazrs]>=2.5.0
reportlab>=4.0.0

# --- the web app and the analyst -------------------------------------------
# `heatcanyon serve` needs these; the notebooks do not.
fastapi>=0.110.0
uvicorn>=0.27.0
# The analyst is Claude Code as a library: heatcanyon/agent/ builds a
# ClaudeAgentOptions, registers twenty in-process MCP tools over the solved
# model, and drives a turn in the background. See docs/AGENT.md.
claude-agent-sdk>=0.2.139
# The single-shot fallback analyst (heatcanyon/ai.py), which needs only an API
# key and is what answers when the agent cannot start.
anthropic>=0.45.0
# Optional, for the validation suite and the tests.
pytest>=8.0.0
